API_FOOTBALL_KEY=replace_with_your_api_key
PORT=8787

# Optional: proxy reachable from the machine running Node (restart after editing).
# HTTPS_PROXY=http://127.0.0.1:7890
# NO_PROXY=localhost,127.0.0.1
