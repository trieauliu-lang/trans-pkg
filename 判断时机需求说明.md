# 判断时机需求说明

## 目标规则

以“山东泰山（主）vs 上海海港（客）”为例：

> 当上半场结束、比赛正式进入中场休息时，只读取中场比分；如果山东泰山比分小于上海海港，则提醒一次。否则不提醒。下半场不再监控这个目标。

判断时机与判断条件是两个独立维度：

1. 判断时机：中场休息（`HT`）。
2. 判断条件：主队比分 `<` 客队比分。

只有两者同时成立才触发提醒。

## 精确状态分类

| 统一状态 | 含义 | 中场规则处理 |
|---|---|---|
| `NS` | 未开始 | 等待 |
| `1H` | 上半场进行中 | 等待，不判断 |
| `HT` | 上半场结束，正在中场休息 | 立即判断一次 |
| `LIVE` | 正在比赛，但半场未知 | 等待更精确状态，不猜测 |
| `PAUSED` | 普通暂停或原因不明 | 不视为中场 |
| `2H` | 下半场进行中 | 标记已错过中场，不补判 |
| `ET` / `BT` / `P` | 加时或点球阶段 | 停止，不补判 |
| `FT` / `AET` / `PEN` | 比赛结束 | 停止，不补判 |
| `PST` / `CANC` / `ABD` / `SUSP` | 延期、取消、腰斩或中止 | 结束规则 |

## 状态机

```text
待监控 -> NS/1H/LIVE/PAUSED -> 继续等待
待监控 -> HT -> 条件成立 -> 提醒一次 -> 完成
待监控 -> HT -> 条件不成立 -> 不提醒 -> 完成
待监控 -> 2H/ET/BT/P/FT/AET/PEN -> 已错过中场 -> 完成
待监控 -> PST/CANC/ABD/SUSP -> 比赛异常结束 -> 完成
```

## 边界规则

- 以供应商状态为准，不按开赛时间或本地时钟猜测中场。
- 只有主客队比分均为非负有限数字时才判断。
- 主队落后严格使用 `homeScore < awayScore`，平局不属于落后。
- 每条规则对每场比赛最多提醒一次。
- 轮询错过短暂 `HT` 时，宁可标记“已错过中场”，也不使用下半场比分补判。
- 数据源只返回模糊 `LIVE` 或普通暂停时继续等待，不擅自归类为 `HT`。
